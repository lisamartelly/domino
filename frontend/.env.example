# Copy to `.env.local` (gitignored) and set to enable in-memory auth mocks — no backend required.
# VITE_USE_MOCK=true

# Registration (set to "false" to temporarily hide the registration form)
VITE_REGISTRATION_ENABLED=true
